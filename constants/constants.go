package constants

const (
	// Name of the CLI app and the command to invoke it.
	AppName = "pal"

	// Relative path to the app’s config directory within a project.
	AppDir = ".pal"

	// Version of the CLI application.
	Version = "v0.2.0"
)
